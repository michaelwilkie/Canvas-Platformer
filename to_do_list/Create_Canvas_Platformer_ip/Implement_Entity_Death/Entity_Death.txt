When an entity dies it should remove itself from the entity list
and all its associated ui elements from the component list
(if it ever gets put into the component list)

A dying animation is optional but should be played if
the entity is a player-like class (can move around and jump and stuff)